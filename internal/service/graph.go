package service

import (
	"github.com/Aysnine/unripe-bison/internal/types"
)

// Graph godoc
// @Summary graphql
// @ID graphql
// @Produce  json
// @Router /api/graph [post]
func SetupApi_Graph(setup *types.SetupContext) {
	// app := setup.App
	// db := setup.DB

	// app.All("/query", func(c *fiber.Ctx) error {
	// 	gqlHandler(c.Context())
	// 	return nil
	// })

	// app.All("/playground", func(c *fiber.Ctx) error {
	// 	playground(c.Context())
	// 	return nil
	// })
}
